
"use client";

// This page has been moved back to /checkout/page.tsx to resolve routing issues.
// The checkout flow no longer uses the orderId in the URL.
export default function CheckoutMovedPage() {
  return null;
}
